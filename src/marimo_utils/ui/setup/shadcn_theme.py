"""Build-time shadcn theme CSS source.

Edit ``styles/shadcn.css`` and rebuild via ``npm run build:css --prefix styles``.
Runtime styling uses the bundled ``dr.css`` artifact from ``stylesheet.py``.
"""

from __future__ import annotations
